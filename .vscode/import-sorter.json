{
    "groupRules": [
        ["^[@][^/]", {}],
        ["^@(/|$)", "^apis?(/|$)", "^typings?(/|$)", "^utils(/|$)"],
        { "flags": "named", "regex": "^[.]" },
        [{ "flags": "scripts" }]
    ],
    "wrappingStyle": "prettier"
}
